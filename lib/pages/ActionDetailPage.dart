// ActionDetailPage.dart
import 'package:flutter/material.dart';

class ActionDetailPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Action Detail"),
      ),
      body: Center(
        child: Text("Details for Action Genre"),
      ),
    );
  }
}
